
const initialState = {
    entities: {
        commits: {},
        developers: {}
    },
    result: []
}

const reducer = ( state = initialState, action) => {
    switch ( action.type) {

    }
    return state;
}

export default reducer;